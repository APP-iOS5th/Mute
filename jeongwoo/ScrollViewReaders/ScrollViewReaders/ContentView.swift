import SwiftUI

struct ContentView: View {
    @State private var scrolledID: CharacterInfo.ID?
    
    var body: some View {
        VStack {
            
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
